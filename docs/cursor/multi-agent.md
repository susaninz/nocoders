# 🤖 Мультиагентная среда в Cursor

> Как работать с несколькими AI-агентами в одном проекте

*Добавлено: 2024-12-09*

---

## Зачем это нужно

- **Экономия контекста** — каждый агент в своём окне, не мешает другим
- **Разделение задач** — один думает, другой кодит, третий проверяет
- **Параллельная работа** — можно вести несколько задач одновременно

---

## Быстрый старт

### 1. Клонируй репозиторий с промптами

```bash
cd ~/Cursor  # или где у тебя проекты
git clone https://github.com/susaninz/nocoders.git
```

Теперь у тебя есть папка `nocoders/` с готовыми промптами агентов.

### 2. Добавь в workspace своего проекта

В Cursor открой свой проект, затем:

```
File → Add Folder to Workspace → выбери папку nocoders/
```

Теперь в sidebar видишь и свой проект, и nocoders:

```
WORKSPACE
├── my-project/          ← твой проект
└── nocoders/
    └── agency/
        └── staff/
            └── напарник.md   ← промпты тут
```

### 3. Создай Agent окно и подключи промпт

1. Открой новое Agent окно в Cursor
2. В первом сообщении напиши:

```
@nocoders/agency/staff/напарник.md — работаем по этому промпту
```

Cursor подгрузит файл и AI примет роль из промпта.

### 4. Повтори для других агентов

Для каждой роли — своё Agent окно:

| Agent окно | Промпт |
|------------|--------|
| Напарник | `@nocoders/agency/staff/напарник.md` |
| Разраб | `@nocoders/agency/staff/разраб.md` |
| Ревизор | `@nocoders/agency/freelance/ревизор.md` |

---

## Как устроены Agent окна

В Cursor Agent окна — это отдельные чаты с AI. Каждое окно — независимый контекст.

```
Cursor
├── Agent: "Напарник"        ← обсуждение идей
├── Agent: "Разраб"          ← написание кода
├── Agent: "Ревизор"         ← проверка кода
└── ...
```

**Важно:** Агенты не знают друг о друге. Если нужно передать информацию между ними — копируй вручную или используй общие файлы.

---

## Доступные агенты

### Штатные (базовая команда)

| Агент | Для чего | Промпт |
|-------|----------|--------|
| 🤝 **Напарник** | Обсуждение идей, архитектура | `agency/staff/напарник.md` |
| 🛠️ **Разраб** | Написание кода | `agency/staff/разраб.md` |
| 📜 **Летописец** | База знаний | `agency/staff/летописец.md` |
| 📢 **Глашатай** | Публикации | `agency/staff/глашатай.md` |

### Фрилансеры (по необходимости)

| Агент | Для чего | Промпт |
|-------|----------|--------|
| 🔍 **Ревизор** | Code review | `agency/freelance/ревизор.md` |
| 🔧 **Механик** | DevOps, деплой | `agency/freelance/механик.md` |

---

## Альтернативные способы

### Скопировать промпты к себе

Если не хочешь добавлять nocoders в workspace:

```bash
# Скопируй нужные промпты в свой проект
cp ~/Cursor/nocoders/agency/staff/напарник.md ~/Cursor/my-project/prompts/
```

Потом в Agent окне: `@prompts/напарник.md`

### Кастомизировать под себя

После клонирования можешь менять промпты как хочешь — это твоя локальная копия.

Если хочешь делиться своими изменениями или получать обновления из оригинала — создай свой репозиторий на GitHub.

---

## Частые вопросы

### Могу ли я использовать разные модели для разных агентов?

Да. В каждом Agent окне можно выбрать свою модель (Opus, Sonnet, GPT-4).

### Как обновить промпты?

```bash
cd ~/Cursor/nocoders
git pull
```

### Нужен ли интернет для работы?

Только для первого клонирования. Потом промпты лежат локально.

---

## Полезные ссылки

- [Agency — описание системы агентов](../../agency/README.md)
- [Репозиторий nocoders](https://github.com/susaninz/nocoders)

---

*Источник: практический опыт вайбкодинга с Cursor*
