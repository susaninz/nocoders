# 🤖 Мультиагентная среда в Cursor

> Как работать с несколькими AI-агентами в одном проекте

*Добавлено: 2024-12-09*

---

## Зачем это нужно

- **Экономия контекста** — каждый агент в своём окне, не мешает другим
- **Разделение задач** — один думает, другой кодит, третий проверяет
- **Параллельная работа** — можно вести несколько задач одновременно

---

## Как устроены Agent окна

В Cursor есть Agent окна (не путать с Composer). Каждое окно — отдельный чат с AI.

```
Cursor
├── Agent: "Напарник"        ← обсуждение идей
├── Agent: "Разраб"          ← написание кода
├── Agent: "Ревизор"         ← проверка кода
└── Agent: "Discussion"      ← что-то ещё
```

Каждый Agent — независимый контекст. Они не знают друг о друге.

---

## Как применить промпт к Agent окну

### Способ 1: Ссылка на файл через @

В начале нового Agent чата пишешь:

```
@путь/к/промпту.md — работаем по этому промпту
```

Cursor понимает `@` как ссылку на файл в workspace.

**Пример:**
```
@nocoders/agency/staff/напарник.md — используй этот промпт
```

### Способ 2: Копипаста

Копируешь текст промпта в первое сообщение чата. Работает, но муторно.

### Способ 3: Cursor Rules (автоматически)

Файлы из `.cursor/rules/` проекта подгружаются автоматически во ВСЕ чаты.

```
project/
└── .cursor/rules/
    └── напарник.md   ← применится ко всем Agent окнам
```

⚠️ **Важно:** Rules применяются ко ВСЕМ чатам, нельзя выбрать "этот чат = Напарник, тот = Разраб".

---

## Рекомендуемый подход

**Для мультиагентности используй способ 1:**

1. Добавь папку с промптами в workspace (`File → Add Folder to Workspace`)
2. В каждом новом Agent окне пиши: `@промпт.md — работаем по этому`
3. Каждое окно — свой агент со своей ролью

**Плюсы:**
- Полный контроль — какой агент в каком окне
- Промпты хранятся в одном месте
- Легко обновлять

**Минусы:**
- Надо писать @ в начале каждого чата (копипаста)

---

## Структура промптов (Agency)

Рекомендуемая структура для хранения промптов:

```
nocoders/agency/
├── staff/              # Штатные агенты (всегда нужны)
│   ├── напарник.md     # Обсуждение идей
│   ├── разраб.md       # Написание кода
│   ├── летописец.md    # База знаний
│   └── глашатай.md     # Публикации
│
└── freelance/          # По необходимости
    ├── ревизор.md      # Code review
    └── механик.md      # DevOps
```

Подробнее: [Agency README](../../agency/README.md)

---

## Симлинки для шаринга промптов

Если хочешь чтобы промпты автоматически обновлялись во всех проектах:

```bash
# Создать симлинк
ln -s ~/Cursor/nocoders/agency/staff/напарник.md \
      ~/Cursor/my-project/.cursor/rules/напарник.md
```

Симлинк — это "ярлык". Изменил оригинал → изменилось везде.

---

## Частые вопросы

### Могу ли я использовать разные модели для разных агентов?

Да. В каждом Agent окне можно выбрать свою модель (Opus, Sonnet, GPT-4 и т.д.).

### Агенты видят файлы друг друга?

Нет. Каждый Agent — независимый контекст. Если хочешь передать информацию — копируй вручную или используй общие файлы в проекте.

### Сколько Agent окон можно открыть?

Сколько хочешь. Ограничение только по API-лимитам.

---

## Полезные ссылки

- [Agency — система мультиагентной разработки](../../agency/README.md)
- [Промпт: Напарник](../../agency/staff/напарник.md)

---

*Источник: практический опыт вайбкодинга с Cursor*

